grep for filtering and searching. awk for extracting columns and counting.
AWK splits by spaces by default. In a line like:
there are no spaces, so AWK sees the whole line as one field:
It does not separate WORKERS and 2.
which means cut is used 

explain everything succinctly concisely where no questions can be raised because it just practically answers everything we learnt from this lab and all the questions i asked including delimters everything.