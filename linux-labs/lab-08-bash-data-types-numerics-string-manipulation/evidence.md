1. 10+1 = 11 0+1 = 1       2.  it prints "hello"  3. it doesnt print hello it prints the same existing variables'  value outside the function (global) since we used local when the function ends the local variable also dies and local variables dont update global variables. 4. fleet report and fleet  report 5. web03 and 3       6. it prints web01 instead of printing each element in the array one at a time this is because we didnt use "${servers[@]}" 7. sets the count variable to 0 as default temporarily if its missing or no defined value. and the other one sets the count variable to 0 and saves it if its missing or no defined value. 8.  high  9.  empty 10. first of all count should be assigned like this count=15 because its an int  and the if statement should -eq instead of = 